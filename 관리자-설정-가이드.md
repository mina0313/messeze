# messeze 문의 관리(/admin) 설정 가이드 — 5분

문의 폼 → **구글 시트 자동 저장** → **admin.html**에서 키 입력 후 목록·상태·메모 관리.

## 1. 구글 시트 만들기
1. 회사 구글 계정으로 https://sheets.new 접속 → 새 시트 생성
2. 이름을 `messeze 문의` 정도로 변경

## 2. Apps Script 붙여넣기
1. 시트 메뉴 **확장 프로그램 → Apps Script** 클릭
2. 기존 코드를 지우고 아래 전체를 붙여넣기
3. 첫 줄 `SECRET` 값을 **원하는 관리자 키**로 변경 (admin 접속 시 입력할 비밀번호)

> **이미 설정을 끝낸 경우(코드만 업그레이드):** 시트 → 확장 프로그램 → Apps Script → 기존 코드 전체를 아래로 교체(단, 첫 줄 SECRET은 **기존 키 그대로**) → 저장 → 상단 함수 선택에서 `꾸미기` 고르고 **▶ 실행** → 권한 승인. **재배포 필요 없음** (doPost/doGet 로직 동일). 시트가 대시보드+디자인 시트로 바뀝니다.

```javascript
const SECRET = '여기에-원하는-관리자키';   // ← 꼭 변경! (이미 쓰던 키가 있으면 그대로)
const STATUS = ['신규','연락중','계약','보류','완료'];

function doPost(e){
  const p = JSON.parse(e.postData.contents);
  if (p.action === 'update') {                    // 관리자: 상태/메모 수정
    if (p.key !== SECRET) return out({ok:false});
    const sh = sheet(), r = Number(p.row);
    if (p.status !== undefined) sh.getRange(r, 6).setValue(p.status);
    if (p.memo   !== undefined) sh.getRange(r, 7).setValue(p.memo);
    return out({ok:true});
  }
  // 홈페이지 문의 접수
  sheet().appendRow([new Date(), p.name||'', p.company||'', p.phone||'', p.site||'', '신규', '']);
  return out({ok:true});
}

function doGet(e){
  if (e.parameter.key !== SECRET) return out({ok:false, err:'unauthorized'});
  const v = sheet().getDataRange().getValues();
  const rows = v.slice(1).map((r,i)=>({row:i+2, ts:r[0], name:r[1], company:r[2], phone:r[3], site:r[4], status:r[5], memo:r[6]}));
  return out({ok:true, rows:rows.reverse()});
}

function sheet(){
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sh = ss.getSheetByName('문의');
  if (!sh){ sh = ss.insertSheet('문의'); sh.appendRow(['접수일시','이름','회사명','연락처','홈페이지','상태','메모']); }
  return sh;
}
function out(o){ return ContentService.createTextOutput(JSON.stringify(o)).setMimeType(ContentService.MimeType.JSON); }

/* ── 시트 디자인(영업 관리형): 함수 선택 → 꾸미기 → ▶ 실행 (언제든 다시 실행 가능) ── */
function 꾸미기(){
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  const sh = sheet();
  sh.setTabColor('#2B5CFF');

  // 컬럼: A~G = 홈페이지 자동 접수(순서 바꾸지 말 것) / H~O = 영업 관리(직접 입력)
  const HEAD = ['접수일시','고객명','업체명','연락처','홈페이지','상태','메모/문의내용',
                '접수경로','관심 플랜','견적금액(월)','계약일','입금상태','담당자','불발 사유','진행현황'];
  sh.getRange(1,1,1,HEAD.length).setValues([HEAD]);
  sh.setRowHeight(1, 40);
  sh.setFrozenRows(1);
  sh.setFrozenColumns(3);
  if (sh.getFilter()) sh.getFilter().remove();
  sh.getRange('A1:O1000').createFilter();

  // 줄무늬(밴딩) — 헤더색보다 먼저
  sh.getRange('A1:O1000').getBandings().forEach(b=>b.remove());
  const band = sh.getRange('A1:O1000').applyRowBanding(SpreadsheetApp.BandingTheme.LIGHT_GREY, true, false);
  band.setHeaderRowColor('#101B3A').setFirstRowColor('#FFFFFF').setSecondRowColor('#F4F7FF');

  // 헤더: 자동접수 영역 네이비 / 영업 영역 딥그린
  sh.getRange('A1:O1').setFontColor('#FFFFFF').setFontWeight('bold').setFontSize(11)
    .setVerticalAlignment('middle').setHorizontalAlignment('center');
  sh.getRange('A1:G1').setBackground('#101B3A');
  sh.getRange('H1:O1').setBackground('#0B5C46');

  // 열 너비·서식
  [140,90,150,120,190,90,260,100,120,120,100,100,90,110,220].forEach((w,i)=>sh.setColumnWidth(i+1,w));
  sh.getRange('A2:O1000').setVerticalAlignment('middle');
  sh.getRange('A2:A1000').setNumberFormat('yyyy-MM-dd HH:mm');
  sh.getRange('K2:K1000').setNumberFormat('yyyy-MM-dd');
  sh.getRange('J2:J1000').setNumberFormat('₩#,##0');
  sh.getRange('F2:F1000').setHorizontalAlignment('center').setFontWeight('bold');
  sh.getRange('H2:I1000').setHorizontalAlignment('center');
  sh.getRange('L2:N1000').setHorizontalAlignment('center');
  sh.getRange('G2:G1000').setWrap(true);
  sh.getRange('O2:O1000').setWrap(true);

  // 드롭다운
  const dv = list => SpreadsheetApp.newDataValidation().requireValueInList(list, true).setAllowInvalid(false).build();
  sh.getRange('F2:F1000').setDataValidation(dv(STATUS));
  sh.getRange('H2:H1000').setDataValidation(dv(['홈페이지','전화','카톡','메일','소개','기타']));
  sh.getRange('I2:I1000').setDataValidation(dv(['베이직(60만)','스탠다드(100만)','글로벌','미정']));
  sh.getRange('L2:L1000').setDataValidation(dv(['입금 대기','입금 완료','해당 없음']));
  sh.getRange('N2:N1000').setDataValidation(dv(['가격','타사 선택','연락 두절','시기 미룸','기타']));

  // 상태·입금상태 자동 색
  const mk = (rng,s,bg,fg)=>SpreadsheetApp.newConditionalFormatRule().whenTextEqualTo(s)
    .setBackground(bg).setFontColor(fg).setRanges([rng]).build();
  const fr = sh.getRange('F2:F1000'), lr = sh.getRange('L2:L1000');
  const rules = [];
  [['신규','#E8F0FE','#1A56DB'],['연락중','#FEF3C7','#92400E'],['계약','#DEF7EC','#046C4E'],['보류','#F3E8FF','#6B21A8'],['완료','#F3F4F6','#6B7280']]
    .forEach(c=>rules.push(mk(fr,c[0],c[1],c[2])));
  [['입금 대기','#FDE8E8','#C81E1E'],['입금 완료','#DEF7EC','#046C4E']]
    .forEach(c=>rules.push(mk(lr,c[0],c[1],c[2])));
  sh.setConditionalFormatRules(rules);

  // ── 요약(KPI 대시보드) 시트 ──
  let goal = 3000000;                             // 이번 달 목표 기본값
  const old = ss.getSheetByName('요약');
  if (old){ const g = old.getRange('H6').getValue(); if (typeof g === 'number' && g > 0) goal = g; ss.deleteSheet(old); }
  const so = ss.insertSheet('요약', 0);
  so.setTabColor('#0BBF8C');
  so.setHiddenGridlines(true);
  so.setColumnWidth(1, 24);
  for (let c = 2; c <= 9; c++) so.setColumnWidth(c, 118);

  so.getRange('B2').setValue('messeze 영업 현황').setFontSize(20).setFontWeight('bold').setFontColor('#101B3A');
  so.getRange('B3').setValue('자동 집계 — 주황색 "이번 달 목표"만 직접 입력, 나머지는 문의 시트 기준 실시간 갱신')
    .setFontSize(10).setFontColor('#9AA3B2');

  // 1줄: 핵심 지표
  const cards = [
    ['오늘 문의',   '=SUMPRODUCT((문의!A2:A1000<>"")*(TEXT(문의!A2:A1000,"yyyy-MM-dd")=TEXT(TODAY(),"yyyy-MM-dd")))', '0'],
    ['이번 달 문의','=SUMPRODUCT((문의!A2:A1000<>"")*(TEXT(문의!A2:A1000,"yyyy-MM")=TEXT(TODAY(),"yyyy-MM")))', '0'],
    ['전체 문의',   '=COUNTA(문의!A2:A1000)', '0'],
    ['계약',        '=COUNTIF(문의!F2:F1000,"계약")', '0'],
    ['계약률',      '=IF(COUNTA(문의!A2:A1000)=0,0,COUNTIF(문의!F2:F1000,"계약")/COUNTA(문의!A2:A1000))', '0.0%'],
    ['월 구독액(계약)','=SUMIF(문의!F2:F1000,"계약",문의!J2:J1000)', '₩#,##0'],
    ['이번 달 목표', null, '₩#,##0'],
    ['달성률',      '=IF(H6=0,0,G6/H6)', '0.0%'],
  ];
  cards.forEach((c, i) => {
    const col = 2 + i;
    so.getRange(5, col).setValue(c[0]).setFontSize(10).setFontWeight('bold')
      .setFontColor('#5A6577').setBackground('#EEF2F9').setHorizontalAlignment('center');
    const v = so.getRange(6, col).setFontSize(18).setFontWeight('bold').setFontColor('#101B3A')
      .setBackground('#EEF2F9').setHorizontalAlignment('center').setNumberFormat(c[2]);
    if (c[1]) v.setFormula(c[1]); else v.setValue(goal);
  });
  so.getRange('H5:H6').setBackground('#FFF7E8');   // 목표 = 직접 입력 칸(주황)
  so.setRowHeight(5, 24); so.setRowHeight(6, 42);

  // 2줄: 상태별 현황
  so.getRange('B8').setValue('상태별 현황').setFontSize(10).setFontWeight('bold').setFontColor('#9AA3B2');
  [['신규','#E8F0FE','#1A56DB'],['연락중','#FEF3C7','#92400E'],['계약','#DEF7EC','#046C4E'],['보류','#F3E8FF','#6B21A8'],['완료','#F3F4F6','#6B7280']]
    .forEach((s, i) => {
      const col = 2 + i;
      so.getRange(9, col).setValue(s[0]).setFontSize(10).setFontWeight('bold')
        .setFontColor(s[2]).setBackground(s[1]).setHorizontalAlignment('center');
      so.getRange(10, col).setFormula('=COUNTIF(문의!F2:F1000,"' + s[0] + '")').setFontSize(18).setFontWeight('bold')
        .setFontColor(s[2]).setBackground(s[1]).setHorizontalAlignment('center');
    });
  so.setRowHeight(9, 24); so.setRowHeight(10, 42);
}

/* 시트 상단에 'messeze' 메뉴 추가 (다시 꾸미기 버튼) */
function onOpen(){
  SpreadsheetApp.getUi().createMenu('messeze').addItem('시트 다시 꾸미기','꾸미기').addToUi();
}
```

## 3. 웹앱으로 배포
1. 우측 상단 **배포 → 새 배포**
2. 유형 선택(톱니바퀴) → **웹 앱**
3. 설정:
   - 다음 사용자 인증 정보로 실행: **나**
   - 액세스 권한이 있는 사용자: **모든 사용자** ← 중요!
4. **배포** 클릭 → 권한 승인(내 계정 선택 → 고급 → 이동 → 허용)
5. 발급된 **웹 앱 URL** 복사 (https://script.google.com/macros/s/…/exec)

## 4. 사이트에 연결
복사한 URL을 클로드에게 전달하면 아래 두 파일의 `APPS_URL`에 넣고 배포해 드립니다:
- `admin.html` (문의 목록 관리 페이지)
- `index.html` (문의 폼 전송)

## 완료 후
- 관리자 페이지: **https://mina0313.github.io/messeze/admin.html** → 설정한 키 입력
- 문의가 들어오면 시트에도 쌓이고, admin에서 신규/연락중/완료 상태와 메모 관리 가능
- 시트 자체를 열어 필터·공유·알림(도구→알림 규칙: 새 행 추가 시 이메일)도 활용 가능

> 보안: 관리자 키는 Apps Script 안에만 존재하고 사이트 코드(공개 저장소)에는 들어가지 않습니다.

---

# 콘텐츠 관리(CMS) 사용법 — /admin의 "✏️ 콘텐츠 관리" 탭

FAQ · 인터뷰 · 용어사전 · 블로그를 코드 없이 admin에서 직접 수정합니다.
**보는 것은 아무 설정 없이 가능**하고, **저장할 때만** GitHub 토큰이 한 번 필요합니다.

## 저장용 GitHub 토큰 발급 (최초 1회, 2분)

1. github.com에 **mina0313 계정**으로 로그인
2. 우측 상단 프로필 → **Settings** → 왼쪽 맨 아래 **Developer settings**
3. **Personal access tokens → Fine-grained tokens → Generate new token**
4. 설정:
   - Token name: `messeze-admin` (아무거나)
   - Expiration: 1년 등 원하는 기간
   - Repository access: **Only select repositories** → `messeze` 만 선택
   - Permissions → Repository permissions → **Contents: Read and write** (이것만!)
5. **Generate token** → `github_pat_...` 로 시작하는 토큰 복사
6. admin에서 처음 저장할 때 붙여넣으면 그 브라우저에 저장됨 (다시 물어보지 않음)

## 동작 원리

- 저장 → GitHub의 `data/*.json` 파일이 수정됨 → **GitHub Actions가 자동으로 페이지를 다시 빌드** → 1~2분 뒤 사이트 반영
- 반영 확인: https://github.com/mina0313/messeze/actions 에서 초록 체크 확인 가능
- 항목 삭제·복제 후에도 **💾 저장을 눌러야** 실제 반영됩니다

## 주의

- 토큰은 절대 사이트 코드나 저장소에 넣지 말 것 (admin 입력창에만)
- `ID(주소용)` / `주소(영문)` 필드는 페이지 주소가 되므로 영문·하이픈만, 기존 항목 것은 바꾸지 않기
- 여러 명이 동시에 수정하면 "다른 곳에서 먼저 수정됐습니다" 안내가 뜸 → 목록에서 다시 불러온 뒤 재수정
